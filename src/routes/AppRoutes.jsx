import { Routes, Route } from "react-router-dom";

const AppRoutes = () => {
  return (
    <div>
      <Routes>
        <Route path="login" />
      </Routes>
    </div>
  );
};

export { AppRoutes };
